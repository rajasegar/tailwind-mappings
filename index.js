'use strict';

const TAILWIND_CLASSES = require('./src/constants');
const {
  getSpacingUtils,
  getBorderRadiusUtils,
} = require('./src/spacing-utils');

module.exports = {
  TAILWIND_CLASSES,
  getSpacingUtils,
  getBorderRadiusUtils,
};
